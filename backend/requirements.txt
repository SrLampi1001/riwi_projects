# Bioma backend — Python 3.13 runtime dependencies.
# Pinned to versions verified against the python:3.13-slim image; bumps go
# through the Chore: dependency update commit convention.

# --- web framework + server ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- DB driver (async) + pool ---
psycopg[binary,pool]==3.2.3

# --- validation + settings ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- auth crypto ---
PyJWT==2.10.1
argon2-cffi==23.1.0

# --- observability ---
structlog==24.4.0

# --- dev / test ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1